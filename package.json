{
  "name": "birdlearn",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild analytics-entry.jsx --bundle --minify --format=iife --outfile=analytics.js"
  },
  "dependencies": {
    "@vercel/analytics": "^1.5.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
